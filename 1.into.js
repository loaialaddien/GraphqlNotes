//why graphql
//the problem with rest apis 
//with highly related data, restful apis conventions starts to break
//also, when we might need just small info we end up with the whole document because we can't specify what data we want to include and what to exclude 

//what is graphql
// GraphQL is a query language that lets you write queries using an object structure rather than a text string.